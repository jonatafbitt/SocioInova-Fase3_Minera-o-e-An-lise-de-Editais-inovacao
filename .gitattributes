# Normalização de fim de linha — o hash SHA-256 das configs registrado em
# eventos (AD-9) precisa ser estável entre clones/plataformas.
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binários — sem conversão
*.png binary
*.pdf binary
